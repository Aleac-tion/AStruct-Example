# 这是一个模拟Inno的样本展示了V7.0提供的外置API和QT6以及底层代码的契合度
# This is a simulated Inno sample that demonstrates the compatibility of the Open API provided by V7.0 with QT6 and the underlying code.

# **他可以展示AList AStruct AStruct::static_getDir() AStruct::static_splittext() AStruct::static_searchtext()的磨合**
# **That display [AList AStruct AStruct::static_getDir() AStruct::static_splittext() AStruct::static_searchtext()] Coordination between low-level compilation and high-level systems**